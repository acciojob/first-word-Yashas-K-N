function firstWord(s) {
  // your code here
	let r = '';
	for(i of s){
		r += s;
		}
	if( s == ''){
		break;
	}
  return r
}
let res = firstword(s);
console.log(res);

// Do not change the code below

// const s = prompt("Enter String:");
// alert(firstWord(s));
